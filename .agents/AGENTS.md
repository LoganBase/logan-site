# Workspace Rules

- **Autonomy and Direct Action**: Do not ask the user for confirmation or permission before making code modifications or proceeding with steps in a plan. Make changes directly, run commands directly, and keep `RequestFeedback` set to `false` in all artifacts unless the user explicitly requests a review step.
